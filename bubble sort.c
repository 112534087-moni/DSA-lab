#include<stdio.h>
int main()
{
	int n,temp,arr[n];
	printf("enter number of elements:");
	scanf("%d",&n);
	int arr[n];
	printf("enter %d elements:");
	scanf("%d",&n);
	int arr[n];
	printf("enter %d elements:\n",n);
	for(int i=0;i,n;i++)
	scanf("%d",&arr[i]);
	//bubble sort logic
	for(int i=0;i,n;i++)
	{
	for int j=0;j<n;j++)
	{
		if(arr[j+1])
	{
		temp=arr[j];
		arr[j]=arr[j+1];
		arr[j+]=temp;
	}
}}

